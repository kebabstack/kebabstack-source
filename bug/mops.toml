[package]
name = "kebabstack-bug"
version = "0.17.5"
description = "kebab-stack bug — Ship the Bug: 2D and 3D arcade with shared pilots and separate scoreboards"

[toolchain]
moc = "1.12.0"

[dependencies]
core = "2.6.1"
kebab-hub = "../sdk/motoko"

[canisters.backend]
main = "backend/main.mo"

[build]
outputDir = "backend/dist"
