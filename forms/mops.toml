[package]
name = "kebabstack-forms"
version = "0.5.1"
description = "kebab-stack forms — forms in, decisions out: a builder with public links and a review pipeline for what comes in"

[toolchain]
moc = "1.12.0"

[dependencies]
core = "2.6.1"
json = "1.4.0"
kebab-hub = "../sdk/motoko"

[canisters.backend]
main = "backend/main.mo"

[build]
outputDir = "backend/dist"
