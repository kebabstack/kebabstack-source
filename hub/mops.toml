[package]
name = "kebab-stack-hub"
version = "0.35.0"
description = "kebab-stack hub — people, sign-in, apps and backups on a cloud engine"

[toolchain]
moc = "1.12.0"

[dependencies]
core = "2.6.1"
json = "1.4.0"
ecdsa = "8.0.1"
sha2 = "0.1.14"

[canisters.backend]
main = "backend/main.mo"

[build]
outputDir = "backend/dist"
