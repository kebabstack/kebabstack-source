[package]
name = "kebabstack-desk"
version = "0.24.1"
description = "kebab-stack desk — service management on the skewer (catalog, approvals, SLA, hub-wired)"

[toolchain]
moc = "1.12.0"

[dependencies]
core = "2.6.1"
sha2 = "0.1.14"
json = "1.4.0"
kebab-hub = "../sdk/motoko"

[canisters.backend]
main = "backend/main.mo"

[build]
outputDir = "backend/dist"
