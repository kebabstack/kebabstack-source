[package]
name = "kebabstack-vault"
version = "0.2.2"
description = "kebab-stack vault — the pantry: canister snapshots, restore and schedules for the hub and every connected app"

[toolchain]
moc = "1.12.0"

[dependencies]
core = "2.6.1"

[canisters.backend]
main = "backend/main.mo"

[build]
outputDir = "backend/dist"
