[package]
name = "kebabstack-assets"
version = "0.16.0"
description = "kebab-stack assets — the devices of a company: who has what, what happened, with the photo that proved it"

[toolchain]
moc = "1.12.0"

[dependencies]
core = "2.6.1"
json = "1.4.0"
sha2 = "0.1.14"
kebab-hub = "../sdk/motoko"

[canisters.backend]
main = "backend/main.mo"

[build]
outputDir = "backend/dist"
